/* const express = require('express');
const router = express.Router();
const controller = require('../../models/trainerModel');

router.post('/', controller.postTrainer);
router.get('/', controller.getTrainer);
router.put('/:id', controller.putTrainer);
router.delete('/:id', controller.deleteTrainer);


module.exports = router;
 */
